name: morphling
description: "A new Flutter package project."
version: 1.0.0
homepage:

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Dependency Injection
  get_it: ^7.2.0
  
  # Lint
  intl: ^0.19.0

  # Shimmer
  shimmer: ^3.0.0

  # Html to PDF
  html_to_pdf: ^0.8.1

  # Path Provider
  path_provider: ^2.0.11


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:

  assets:
    - assets/manto.jpg
